extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back

    .error-page
        h1
            i.fas.fa-skull-crossbones
            |  Oopsy Daisy, something went wrong: #{error.message}
        h2 Status Code: #{error.status}
        pre= error.stack